<h2>ジャンル編集</h2>
 <%= form_with model: @genre, url:genres_path, method: :post, local: true  do |f| %>
  <div class="form-group">
    <%=f.label :ジャンル名 %>
    <%=f.text_field :name, class: 'form-control', placeholder:"キャンディー" %>
    <%=f.submit '変更を保存', class: 'btn btn-success'%>
  </div>
